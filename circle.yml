machine:
  ruby:
    version: 2.1.0p0
  node:
    version: 0.10.22
dependencies:
  override:
    - 'npm install gulp -g && npm install bower -g && npm install'
    - 'gem install sass -v=3.2.14'
test:
  override:
    - 'gulp bower'
    - 'gulp templatecache'
    - 'gulp specs'
