machine:
  ruby:
    version: 1.9.3-p0-falcon
  node:
    version: 0.10.22
dependencies:
  override:
    - 'npm install gulp -g && npm install bower -g && npm install'
    - 'gem install sass -v=3.2.14'
test:
  override:
    - 'gulp bower'
    - 'gulp templatecache'
    - 'gulp specs'
